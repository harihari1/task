As we disucssed earlier regarding AWS Devops task

Here we have Dockerfile to build docker image.
if we need to configure port number kindly change it in Dockerfile(Expose)
Run the Command "Docker build -t (IMAGE_NAME) (PATH)"
Succesfully created the Docker Image.
Push your Docker Image to Docker Hub Public Repository using credentials 

*For deplying React App in kubernetes use deployment.yaml file 
*If we need to deploy the application using Micro k8s type the command "microk8s kubectl apply -f (Deployment_File_Name)"
*We have deployed React app in Kubernetes 
*Port number are configured to 3000 you can access the application via http://localhost:3000 
